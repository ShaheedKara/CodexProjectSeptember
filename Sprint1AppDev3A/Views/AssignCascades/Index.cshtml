@model IEnumerable<Sprint1AppDev3A.Models.AssignCascade>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BookingId.ContainerNum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TruckId.TrailerSize)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SizeCheck)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BookingId.ContainerNum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TruckId.TrailerSize)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SizeCheck)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssignID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssignID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssignID })
        </td>
    </tr>
}

</table>
