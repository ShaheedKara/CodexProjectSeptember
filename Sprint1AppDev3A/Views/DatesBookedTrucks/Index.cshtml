@model IEnumerable<Sprint1AppDev3A.Models.DatesBookedTrucks>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TrucksId.make)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PickUpDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DropOffDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TrucksId.make)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PickUpDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DropOffDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TrucKey }) |
            @Html.ActionLink("Details", "Details", new { id=item.TrucKey }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TrucKey })
        </td>
    </tr>
}

</table>
